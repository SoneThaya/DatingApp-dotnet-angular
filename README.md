# Dating App - dotnet / Angular

## To start backend

- cd into `API` folder
- in terminal run `dotnet watch --no-hot-reload`

## To Start the Angular Front End

- cd into `client` folder
- run `ng serve`
